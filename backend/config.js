const username = encodeURIComponent("Sushovan");
const password = encodeURIComponent("Belladonn@2358")

export const PORT = 3000;
export const mongoDBURI = `mongodb+srv://${username}:${password}@cluster0.evmms.mongodb.net/blogpost?retryWrites=true&w=majority&appName=Cluster0`;
